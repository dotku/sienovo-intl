---
title: "Linux 0.11 namei.c Comments"
date: "2011-08-01"
slug: "6652042"
tags: ["#Linux", "#Struct", "#Null", "#Buffer", "#Disk", "#Byte"]
source: "https://blog.csdn.net/yeyuangen/article/details/6652042"
originalTitle: "linux 0.11  namei.c 注释"
---

```cpp
/* * linux/fs/namei.c * * (C) 1991 Linus Torvalds */    /* * Some corrections by tytso. */  /* * tytso made some corrections. */    #include <linux/sched.h>  // Scheduler header file, defines the task_struct task structure, data for initial task 0,
   // and some embedded assembly function macros for setting and getting descriptor parameters.
#include <linux/kernel.h> // Kernel header file. Contains prototypes for some common kernel functions.
#include <asm/segment.h>  // Segment operation header file. Defines embedded assembly functions for segment register operations.
#include <string.h>       // String header file. Mainly defines some embedded functions for string operations.
#include <fcntl.h>        // File control header file. Defines constant symbols for file and descriptor operation control.
#include <errno.h>        // Error number header file. Contains various error numbers in the system. (Introduced by Linus from Minix).
#include <const.h>        // Constant symbols header file. Currently only defines the flag bits for the i_mode field in i-nodes.
#include <sys/stat.h>     // File status header file. Contains the stat{} structure and constants for file or file
